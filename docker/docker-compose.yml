services:
  db:
    profiles: ["default", "dev"]
    build:
      context: .
      dockerfile: dockerfile.pq
    volumes:
      - db_data:/var/lib/postgresql/data
    networks:
      docker_valhalla_network:
        ipv4_address: 172.20.0.2
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 2s
      timeout: 1s
      retries: 3

  api:
    profiles: ["default"]
    build:
      context: .
      dockerfile: dockerfile.api
    env_file:
      - .env
    networks:
      docker_valhalla_network:
        ipv4_address: 172.20.0.3
    depends_on:
      db:
        condition: service_healthy  # Wait for db service to be healthy
    restart: always
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/postgres
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080 || exit 1"]
      interval: 30s
      timeout: 2s
      retries: 3

  api_dev:
    profiles: ["dev"]
    build:
      context: .
      dockerfile: dockerfile.api_dev
    env_file:
      - .env
    networks:
      docker_valhalla_network:
        ipv4_address: 172.20.0.4
    volumes:
      - src_data:/usr/src/
    depends_on:
      db:
        condition: service_healthy  # Wait for db service to be healthy
    restart: always
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/postgres
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080 || exit 1"]
      interval: 30s
      timeout: 2s
      retries: 3

  client_dev:
    profiles: ["dev_"]
    build:
      context: .
      dockerfile: dockerfile.client_dev
    env_file:
      - .env
    networks:
      docker_valhalla_network:
        ipv4_address: 172.20.0.5
    depends_on:
      api_dev:
        condition: service_healthy  # Wait for db service to be healthy
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://172.20.0.4:8080 || exit 1"]
      interval: 30s
      timeout: 2s
      retries: 3

networks:
  docker_valhalla_network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16

volumes:
  db_data:
  src_data:
