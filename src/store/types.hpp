#pragma once

// Enum for object types
enum class Type {
    DatabaseConnectionPool,
    DatabaseController,
    RestHelper,
    SessionManager,
    TokenManager,
    PasswordCrypt,
    PatientController,
    ClinicController,
    UserController,
    ProviderController,
    DOSDetector
};
